# diabetes_prediction_application

#Developed using MAchine Learning Algorithms on Diabetes prediction dataset 

#Model is developed by training using a ML algorithm 

#Model is stored as pickle (pkl) format named as model.pkl

#frontend using flask and gradio 

#Gradio is a Python library that makes it super easy to create web-based user interfaces (UIs) for your machine learning models, functions, or applications — with very little code.
